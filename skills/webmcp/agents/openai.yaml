interface:
  display_name: "WebMCP Browser"
  short_description: "Open in-app pages and use MCP first"
  default_prompt: "Use $webmcp with the URL and full request, wait for sign-in if needed, then use the host bridge, WebMCP relay, or browser JavaScript evaluator before UI automation."
